name: raices_vivas
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <=3.8.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.40.0
  cupertino_icons: ^1.0.8
  video_player: ^2.7.0  # Paquete para reproducir videos
  flutterfire_cli: ^1.2.0
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
